#!/usr/bin/node

const modulesToKeywords = require('../lib/modulesToKeywords');

const file = process.argv[2];
const outBase = process.argv[3]
const keywordStatsFile = process.argv[4];

modulesToKeywords(file, outBase, keywordStatsFile);
